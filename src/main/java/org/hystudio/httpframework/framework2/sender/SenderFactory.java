package org.hystudio.httpframework.framework.sender;

import org.hystudio.httpframework.framework.Session;
import org.hystudio.httpframework.framework.config.RequestMethod;

public class SenderFactory {
    public static ISender createSender(Session session) {
        /*if (session.getReques.equals(RequestMethod.GET)) {
            return new GetSender();
        }*/
        return null;
    }
}
